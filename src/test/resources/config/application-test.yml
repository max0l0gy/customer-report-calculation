delay-in-millis:
  options: [200]
wiremock:
  server:
    port: 4567

#Open-feign client
feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 20000
        loggerLevel: full